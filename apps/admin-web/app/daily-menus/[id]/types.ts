/**
 * Type aliases to distinguish food components from React components
 */
import { ComponentDto, PackComponentDto } from '@contracts/core';

// Food component types (not React components)
export type FoodComponentDto = ComponentDto;
export type PackFoodComponentDto = PackComponentDto;
