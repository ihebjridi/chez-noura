# Database Configuration
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/db_chez-noura

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# Server Configuration
PORT=3000
NODE_ENV=production

# CORS Configuration (comma-separated list of allowed origins, optional)
# If not set, defaults to localhost (dev) and cheznoura.com subdomains (prod)
# CORS_ORIGINS=https://admin.cheznoura.com,https://business.cheznoura.com,https://client.cheznoura.com
